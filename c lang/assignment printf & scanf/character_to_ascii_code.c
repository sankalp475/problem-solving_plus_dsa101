#include <stdio.h>

int  main(void) {


    char x,y,z;
	printf("enter the three character's to gets its ascii:  ");
	scanf("%c,%c,%c",&x,&y,&z);
	printf("the ascii code character '%c' = %d, '%c' = %d, '%c' = %d",x,x,y,y,z,z);
	printf("\n ");
	return 0;
}