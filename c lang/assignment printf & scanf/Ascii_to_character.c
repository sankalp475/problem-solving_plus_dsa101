#include <stdio.h>

int  main(void) {

    int acsii_code;
	printf("Enter the <acsii_code> to get corosponding character: ");
	scanf("%d",&acsii_code);
    printf(" the ascii code is %d and character is %c\n",acsii_code,acsii_code);
	printf("\n");
	return 0;
}